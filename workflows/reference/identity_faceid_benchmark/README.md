# Identity Benchmark — IPAdapter FaceID SD1.5

**Status:** Package 4.12 benchmark-only (CODE/SIM plumbing)

**Candidate:** ipadapter_faceid_sd15_benchmark

This workflow JSON is a **bounded SD1.5 staging stub** for prepare/open/restage simulations.
Live operational benchmarking requires the candidate custom nodes and weights to be present and the graph to be executed for scenarios S1–S4 with human rubric review.

Prepare/open alone does **not** quality-benchmark the method.
