# outpainting

**Status:** Implemented — Package 4.8 Workflow Library (`base/outpainting`)

## Purpose

Extend an image canvas while preserving composition.

## Library

- Manifest: `manifest.json`
- Prepare: `--workflow base/outpainting` with `left`/`right`/`top`/`bottom` (at least one > 0)
- Production status: **partial**

See [docs/workflow-library.md](../../../docs/workflow-library.md).
