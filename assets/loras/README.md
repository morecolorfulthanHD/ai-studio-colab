# LoRAs

LoRA adapter weight files for style, character, and concept fine-tuning.

Use-case-specific LoRAs (e.g., character LoRAs) belong in `use_cases/<project>/` unless shared across multiple projects.

Platform LoRAs used by base workflows are registered in `configs/models/`.
