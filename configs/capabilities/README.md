# Capability Registry

Capability definitions for AI Studio.

**Manifest:** `capability_registry.json`

Capabilities are user-facing functionality abstractions. They are not models, nodes, assets, or workflows.

```bash
python core/scripts/validate_capabilities.py
python core/scripts/validate_capabilities.py --summary
python core/scripts/validate_capabilities.py --capability txt2img
```
