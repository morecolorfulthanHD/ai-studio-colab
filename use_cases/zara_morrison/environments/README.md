# Environments

Environment capture sets for Zara Morrison scene validation.

## Planned Categories

| Category | Purpose |
|----------|---------|
| `interior/` | Indoor environment references |
| `exterior/` | Outdoor environment references |
| `multi_view/` | Multiple angles of the same environment for reconstruction tests |

Used as inputs to `workflows/pipelines/environment_generation/` and `environment_reconstruction/`.
